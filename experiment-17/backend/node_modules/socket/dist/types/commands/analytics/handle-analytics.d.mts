import type { OutputKind } from '../../types.mts';
export type HandleAnalyticsConfig = {
    filepath: string;
    outputKind: OutputKind;
    repo: string;
    scope: string;
    time: number;
};
export declare function handleAnalytics({ filepath, outputKind, repo, scope, time }: HandleAnalyticsConfig): Promise<void>;
//# sourceMappingURL=handle-analytics.d.mts.map