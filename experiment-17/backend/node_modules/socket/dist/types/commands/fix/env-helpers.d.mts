import type { PrMatch } from './pull-request.mts';
import type { RepoInfo } from '../../utils/git.mts';
export interface FixEnv {
    baseBranch: string;
    gitEmail: string;
    githubToken: string;
    gitUser: string;
    isCi: boolean;
    prs: PrMatch[];
    repoInfo: RepoInfo | undefined;
}
export interface MissingEnvVars {
    missing: string[];
    present: string[];
}
/**
 * Get formatted instructions for setting CI environment variables.
 */
export declare function getCiEnvInstructions(): string;
/**
 * Check which required CI environment variables are missing.
 * Returns lists of missing and present variables.
 */
export declare function checkCiEnvVars(): MissingEnvVars;
export declare function getFixEnv(): Promise<FixEnv>;
//# sourceMappingURL=env-helpers.d.mts.map