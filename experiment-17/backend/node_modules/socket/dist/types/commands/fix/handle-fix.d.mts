import type { FixConfig } from './types.mts';
import type { OutputKind } from '../../types.mts';
import type { Remap } from '@socketsecurity/registry/lib/objects';
export type HandleFixConfig = Remap<FixConfig & {
    applyFixes: boolean;
    ghsas: string[];
    glob: string;
    orgSlug: string;
    outputKind: OutputKind;
    unknownFlags: string[];
    outputFile: string;
}>;
/**
 * Converts mixed CVE/GHSA/PURL IDs to GHSA IDs only.
 * Filters out invalid IDs and logs conversion results.
 */
export declare function convertIdsToGhsas(ids: string[]): Promise<string[]>;
export declare function handleFix({ applyFixes, autopilot, cwd, ghsas, glob, limit, minSatisfying, orgSlug, outputFile, outputKind, prCheck, rangeStyle, spinner, unknownFlags }: HandleFixConfig): Promise<void>;
//# sourceMappingURL=handle-fix.d.mts.map