import type { FOLD_SETTING, REPORT_LEVEL } from './types.mts';
import type { OutputKind } from '../../types.mts';
export type HandleScanReportConfig = {
    orgSlug: string;
    scanId: string;
    includeLicensePolicy: boolean;
    outputKind: OutputKind;
    filepath: string;
    fold: FOLD_SETTING;
    reportLevel: REPORT_LEVEL;
    short: boolean;
};
export declare function handleScanReport({ filepath, fold, includeLicensePolicy, orgSlug, outputKind, reportLevel, scanId, short }: HandleScanReportConfig): Promise<void>;
//# sourceMappingURL=handle-scan-report.d.mts.map